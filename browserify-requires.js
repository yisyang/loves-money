// Generated by CoffeeScript 1.8.0
(function() {
  var config, crypto, hmacSha1, sha1;

  crypto = require('crypto');

  sha1 = require('crypto-js/sha1');

  hmacSha1 = require('crypto-js/hmac-sha1');

  config = {};

  config.client_encryption_key = sha1('thisKeyIsMeantToBePublicAnd!AllThatSecure').toString().substring(0, 32);

  window.LM = {};

  window.LM.sha1 = function(raw) {
    return sha1(raw).toString();
  };

  window.LM.hmacSha1 = function(raw, secret) {
    return hmacSha1(raw, secret).toString();
  };

  window.LM.encrypt = function(raw, key, iv) {
    var cipher;
    cipher = crypto.createCipheriv('aes-256-cbc', key, iv);
    cipher.update(raw, 'utf8');
    return cipher.final('base64');
  };

  window.LM.decrypt = function(encrypted, key, iv) {
    var decipher;
    decipher = crypto.createDecipheriv('aes-256-cbc', key, iv);
    decipher.update(encrypted, 'base64');
    return decipher.final('utf8');
  };

}).call(this);

//# sourceMappingURL=browserify-requires.js.map
